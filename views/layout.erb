<!DOCTYPE html>
<html lang="ja">
<head>
	<meta charset="UTF-8">
	<title>MusicSNS</title>
</head>
<body>
	<nav>
		<div>
			<div>
				<a href="/">MusicSNS</a>
			</div>
			<ul>
      <% if !current_user.nil? %>
        <p>こんにちは、<%= current_user.name %>さん</p>
				<p><img src="<%= current_user.img %>"></p>
        <p><a href="/signout"> ログアウト</a></p>
      <% else %>
				<li>新規ユーザーですか?</li>
				<li><a href="/signin">ログイン</a></li>
				<li><a href="/signup">新規登録</a></li>
      <% end %>
			</ul>
		</div>
	</nav>
	<%= yield %>
</body>
</html>